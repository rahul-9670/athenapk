# Generated EOS tables. The shipped device table src/eos/eos_table.bin is committed
# (tracked; not ignored here). The HDF5 sidecars and the Phase-3 hi-res candidate are
# regenerable via gen_eos_table.py and too large to version.
*.h5
eos_table_hires.bin
# 2026-08-08: eos_table_hires_v2.bin is the FLAGSHIP's table (root_ladder + all 24 ensemble
# members name it explicitly) and is the same 12.5 MB class as the v1 hires file above, so it
# follows the same policy. Its dimensions, md5 and exact regeneration command are recorded in
# TABLE_PROVENANCE.md -- without that record "regenerable" is an assertion, not a procedure.
eos_table_hires_v2.bin
